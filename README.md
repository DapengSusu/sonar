# sonar

### build

``` sh
mkdir build & cd build
cmake ..
make
```

### run

``` sh
ls build/bin
```

1. one shell run:
``` sh
./build/bin/sonar_server
```

2. another shell run:
``` sh
./build/bin/sonar_client
```
